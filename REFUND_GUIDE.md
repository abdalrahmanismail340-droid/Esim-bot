# نظام استرجاع الرصيد (Refund System) — شرح كامل

## للعملاء 👤

العميل اللي عنده رصيد في الحساب بتاعه يقدر يسترجعه في أي وقت:

**الخطوات:**
1. اضغط 💰 **رصيدي** من القائمة الرئيسية
2. اضغط 💸 **استرجاع رصيد**
3. ادخل المبلغ (أو اكتب "كل" للرصيد كله)
4. ابعت Binance ID بتاعك أو USDT address (محفظتك)
5. اكتب السبب (اختياري)
6. أكّد الطلب

الطلب هيوصل للأدمن على طول بينبيهه.

---

## للأدمن ⚙️

### عرض الطلبات
من لوحة التحكم → 💳 **استرجاع الرصيد**

كل طلب معلق فيه:
- 👤 يوزرنيم العميل
- 💵 المبلغ
- 🆔 Binance ID أو محفظة USDT
- 💬 السبب اللي بعته

### الموافقة ✅

اضغط على الطلب:
- اضغط **✅ الموافقة والتحويل**
- ابعت:
  - **Binance Txid** (معرف المعاملة من بينانس)
  - أو **auto** للتحويل التلقائي (لو عندك API keys مشغلة)
  - أو **pending** لو بتحول بعدين يدويًا

البوت هيخزّن المعاملة ويبلغ العميل.

### الرفض ❌

اضغط على الطلب:
- اضغط **❌ الرفض**
- اكتب السبب (اختياري)
- الرصيد هيترجع للعميل تلقائي

---

## التحويل التلقائي (Binance API) 🤖

لو بتختار **auto**، البوت هيحول الفلوس من حسابك في بينانس مباشرة.

**محتاج:**
1. حساب Binance
2. تفعيل Withdrawal API من Settings
3. `BINANCE_API_KEY` و `BINANCE_API_SECRET` في متغيرات البيئة

**إيه اللي بيحصل:**
- البوت بيرسل طلب للـ Binance withdrawal API
- الفلوس بتروح للـ address اللي بعتها العميل
- معرّف المعاملة (Txid) بيتحفظ تلقائي

---

## متابعة التحويلات 📊

البوت في الخلفية (كل 5 دقائق) بيفحص حالة التحويلات اللي ما اكتملتش لسه:

✅ لما تكتمل → العميل بياخد رسالة: "تم استقبال المبلغ"

❌ لو فشل → الرسالة بتقول الخطأ

---

## قاعدة البيانات 💾

جدول `refund_requests`:
- `id` — معرّف الطلب
- `user_id` — ID العميل
- `amount` — المبلغ
- `binance_id` — يوزرنيم بينانس أو USDT address
- `status` — pending / approved / rejected
- `txid` — معرّف المعاملة (لما يتحول)
- `reason` — سبب الطلب
- `admin_note` — ملاحظة الأدمن عند الرفض
- `created_at` — وقت الطلب
- `processed_at` — وقت المعالجة

---

## أمثلة

### مثال 1: عميل بدون Binance
```
عميل: عايز استرجاع رصيدي (50$)
عميل: Binance ID: cairo_sales
عميل: شراء من غلط

أدمن: اضغط ✅ الموافقة والتحويل
أدمن: ابعت: 0xXYZ123 (معرف المعاملة من حسابي)
عميل: ✅ اتحول 50$ — Txid: 0xXYZ123
```

### مثال 2: تحويل تلقائي
```
عميل: عايز استرجاع 25$
عميل: USDT address: 0xABC...

أدمن: اضغط ✅ الموافقة والتحويل
أدمن: ابعت: auto
بوت: تحويل تلقائي → Binance API → معرّف المعاملة
عميل: ✅ اتحول 25$ تلقائي
```

### مثال 3: طلب مرفوض
```
عميل: عايز استرجاع 100$

أدمن: اضغط ❌ الرفض
أدمن: السبب: الرصيد اتحط كـ promotion ما يتسحبش
عميل: ❌ اترفض الطلب — الرصيد اترجع
```

---

## المتغيرات البيئية

```bash
# لـ Binance API (للتحويل التلقائي)
BINANCE_API_KEY=your_key_here
BINANCE_API_SECRET=your_secret_here

# لـ البوت (إجباري)
BOT_TOKEN=...
ADMIN_IDS=...
```

اتركهم فاضيين لو ما عندك Binance API، هتشتغل يدويًا بدون مشاكل.

